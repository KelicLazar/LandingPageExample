{
  "name": "popart-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-sass": "sass --watch scss/*.scss:css/*.css",
    "start": "live-server",
    "scss": "node-sass -w scss/styles.scss -o css/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.69.5"
  },
  "devDependencies": {
    "node-sass": "^9.0.0"
  }
}
